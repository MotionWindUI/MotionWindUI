"use client";

import React from "react";
//import { MotionWindUIBaseProps } from "@motionwindui/base";

export interface {{capitalize componentName}}Props {}

const {{capitalize componentName}} = React.forwardRef((props: {{capitalize componentName}}Props, ref: React.ForwardedRef<HTMLDivElement>) => {
  return <></>;
});

{{capitalize componentName}}.displayName = "MotionWindUI.{{capitalize componentName}}";

export default {{capitalize componentName}};
